E: Sub-process /usr/bin/dpkg returned an error code (1)
sudo dpkg --configure -a
sudo apt-get install -f
# install pip python 2.7
curl https://bootstrap.pypa.io/get-pip.py --output get-pip.py
sudo python2 get-pip.py
pip2 --version

# memuat ulang bashrc
source ~/.bashrc

# mencari file
find -iname "namaberkas"
find -name "namaberkas" ---> sensitive case
find / -iname "namaberkas" ---> mencari di root
find /home -iname "*.txt" ---> mencari di home semua file txt


# install php dan apache
sudo apt-get install php libapache2-mod-php php-mysql php-curl php-gd php-json php-pear php-dev php-xmlrpc php-zip php-fpm php-xml php-mbstring apache2
    
# remove apache
sudo apt-get remove --purge php* libapache2* apache2*
sudo apt-get clean


# remove nginx
sudo apt-get remove nginx nginx-common  # Removes all but config files.

sudo apt-get purge nginx nginx-common  # Removes everything.

#gstreamer

sudo apt-get install libgstreamer1.0-0 gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav gstreamer1.0-doc gstreamer1.0-tools gstreamer1.0-x gstreamer1.0-alsa gstreamer1.0-gl gstreamer1.0-gtk3 gstreamer1.0-qt5 gstreamer1.0-pulseaudio


# install composer
curl -sS https://getcomposer.org/installer | php
mv composer.phar /usr/local/bin/composer

#install file tar.gz
#install java
sudo mkdir /opt/java --> membuat folder baru
sudo tar -zxf java.tar.gz -C /opt/java

#install intelljidea
sudo mkdir /opt/intelljidea
sudo tar xfz intelljidea.tar.gz -C /opt/intelljidea

#install jupyter notebook

sudo apt-get install python3-pip python3-dev
sudo apt-get install -y python3-setuptools

python3 -m pip install --upgrade pip
python3 -m pip install jupyter
python3 -m pip install jupyter-core
jupyter notebook

# uninstall jupyter notebook
python3 -m pip uninstall -y jupyter jupyter_core jupyter-client jupyter-console notebook qtconsole nbconvert nbformat



# path xampp
export PATH="/opt/lampp/bin:$PATH"

#run xampp
sudo /opt/lampp/lampp start    

#mematikan aplikasi
ps aux
kill (pid)
kill -9 (pid) -> mematikan dengan paksa

ps aux | grep gdebi --> mencari pid aplikasi tertentu

# menyetting waktu local
timedatectl set-local-rtc 1  --> 0 untuk utc

# menampilkan file
ls-l
ls-la  -> menampilkan file tersembunyi

# menghapus file
rm [namafile]
rmdir [nama folder]
rm -rf [nama folder] --> menghapus isi folder beserta isinya

# mencopy file
cp nama_folder tujuan_folder
cp nama_file tujuan_folder
cp -R nama_file tujuan -> folder bla belum ada akan dibuatkan secara otomatis

# akses file
sudo chmod 777 nama_file/nama_folder  -->hak akses penuh
sudo chmod -R 777 nama_folder   -->hak akses penuh folder beserta isinya

# mengatasi Port yang sudah digunakan
sudo fuser -k 8000/tcp

#Environment Variabel bash

set | less
